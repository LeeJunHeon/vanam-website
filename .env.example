# 견적 요청 알림용 Google Chat 웹훅 URL
# - 실제 값이 담긴 .env 파일은 절대 커밋하지 않는다 (.gitignore에 등록됨 — 공개 저장소)
# - 로컬 개발: 이 파일을 .env로 복사해 값을 채우면 dev 서버에서 실제 전송된다.
# - Cloudflare 배포: 대시보드 Settings > Variables and Secrets 에 같은 이름으로 등록한다.
# - 값이 없으면 /api/quote 는 전송하지 않고 서버 콘솔에 내용만 출력한다(로컬 테스트용).
GOOGLE_CHAT_WEBHOOK=""
